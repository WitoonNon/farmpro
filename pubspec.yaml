name: farmpro
description: "FarmPro - Smart Agriculture Store Application"
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # State Management
  provider: ^6.1.2

  # UI Components
  carousel_slider: ^4.2.1
  smooth_page_indicator: ^1.1.0

  # Fonts
  google_fonts: ^6.2.1

  # Image
  cached_network_image: ^3.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
